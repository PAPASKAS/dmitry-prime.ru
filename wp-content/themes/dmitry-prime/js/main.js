"use strict";

new WOW().init();

window.addEventListener("load", function () {
   const preloaderArea = document.getElementsByClassName('preloader-area')[0];
   preloaderArea.style.display = "none";
});
